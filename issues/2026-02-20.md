# Points Language Issues — 2026-02-20

### Issue 15: Spec directory renamed from `points/` to `spec/` [RESOLVED]

**Scenario**: The spec directory was named `points/`, matching the language name. But `spec/` is a clearer, more conventional name that immediately communicates the directory's purpose — it holds the design specification — without requiring knowledge of the Points language.
**Proposal**: Rename the conventional spec directory from `points/` to `spec/` in all language-level references: config.md template, SKILL.md description and file structure example, workflow.md Phase 1 scope.
**Resolution**: Updated config.md, SKILL.md, workflow.md, and README.md. Historical issue/changelog entries left unchanged.
